<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000000"
    android:fitsSystemWindows="true">

    <!-- Top spacer -->
    <View
        android:layout_width="match_parent"
        android:layout_height="20dp" />

    <!-- Large header -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Limits"
        android:textSize="34sp"
        android:fontFamily="sans-serif-black"
        android:letterSpacing="-0.02"
        android:textColor="#FFFFFF"
        android:paddingHorizontal="24dp"
        android:paddingTop="16dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Set boundaries for yourself"
        android:textSize="14sp"
        android:fontFamily="sans-serif"
        android:textColor="#98989D"
        android:paddingHorizontal="24dp"
        android:paddingTop="4dp"
        android:paddingBottom="16dp" />

    <!-- Tip card -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_card_white"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="20dp"
        android:padding="20dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="How it works"
            android:textColor="#000000"
            android:fontFamily="sans-serif-medium"
            android:textSize="14sp"
            android:layout_marginBottom="6dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Enter a daily limit in minutes for any app. Once the limit is reached, the app will be blocked for the rest of the day."
            android:textColor="#48484A"
            android:fontFamily="sans-serif"
            android:textSize="13sp"
            android:lineSpacingExtra="3dp" />
    </LinearLayout>

    <!-- Section label -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SET PER-APP LIMITS"
        android:textColor="#98989D"
        android:fontFamily="sans-serif-medium"
        android:textSize="11sp"
        android:letterSpacing="0.12"
        android:paddingHorizontal="24dp"
        android:paddingBottom="12dp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="20dp"
        android:clipToPadding="false"
        android:paddingBottom="24dp" />

</LinearLayout>
